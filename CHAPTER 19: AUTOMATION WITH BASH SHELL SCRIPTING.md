```markdown
# 🐚 CHAPTER 19: AUTOMATION WITH BASH SHELL SCRIPTING

مرجع جامع و یکپارچه مبانی خودکارسازی با بش اسکریپت (Bash Shell Scripting)، متغیرها، پارامترهای ورودی، ساختارهای شرطی (`if-then-else`)، حلقه‌های تکرار (`for`/`while`) و آزمون‌های منطقی (`test` / `[ ]`) برای آزمون RHCSA 9.

---

## 📄 PART 1: THE MANUAL (کتابچه مفاهیم و قواعد اسکریپت‌نویسی)

### 📜 ۱. آناتومی و اجرای اسکریپت (The Shebang & Permissions)
یک اسکریپت بش استاندارد باید قواعد زیر را رعایت کند:
* **خط Shebang (`#!/bin/bash`):** اولین سطر اسکریپت که به سیستم‌عامل اعلام می‌کند کدام مفسر (Interpreter) برای اجرای این فایل متنی استفاده شود.
* **مجوز اجرا (Execute Bit):** فایل‌های اسکریپت به صورت پیش‌فرض مجوز اجرا ندارند و باید با `chmod +x script.sh` اجرایی شوند.
* **موقعیت استاندارد اجرا:** برای اجرای اسکریپت در مسیر جاری باید از `./script.sh` استفاده شود (مگر اینکه مسیر آن در متغیر `$PATH` سیستم مانند `/usr/local/bin` تعریف شده باشد).

---

### 📦 ۲. متغیرها، جایگزینی فرمان و پارامترهای ورودی
* **متغیرهای سفارشی:** تعریف بدون فاصله انجام می‌شود: `TARGET_DIR="/var/log"`. فراخوانی با `$TARGET_DIR` یا `${TARGET_DIR}` است.
* **جایگزینی دستور (Command Substitution):** ذخیره خروجی یک دستور درون یک متغیر با ساختار `$(command)` انجام می‌شود (مثلاً `TODAY=$(date +%F)`).
* **پارامترهای موقعیتی (Positional Parameters):**
  * `$0`: نام فایل اسکریپت در حال اجرا.
  * `$1`, `$2`, `$3` ...: آرگومان‌های ورودی پاس‌داده‌شده از خط فرمان به اسکریپت.
  * `$#`: تعداد کل آرگومان‌های ورودی پاس‌داده‌شده.
  * `$@`: تمام آرگومان‌های ورودی به صورت لیست تفکیک‌شده.
  * `$?`: کد خروج (Exit Status) آخرین دستور اجرا شده (`0` یعنی موفقیت، هر مقداری غیر از صفر یعنی خطا).

---

### ⚖️ ۳. ساختارهای شرطی و عملگرهای مقایسه‌ای (`if` / `test`)
در زبان بش، بلوک شرطی با `if [ condition ]; then` آغاز و با `fi` خاتمه می‌یابد:
* **بررسی فایل‌ها و دایرکتوری‌ها:**
  * `-e <path>`: وجود داشتن فایل یا دایرکتوری.
  * `-f <path>`: وجود داشتن یک فایل عادی (Regular File).
  * `-d <path>`: وجود داشتن یک دایرکتوری.
  * `-s <path>`: فایل وجود دارد و حجم آن بزرگتر از صفر بایت است.
* **بررسی رشته‌ها (String Comparisons):**
  * `-z "$str"`: بررسی خالی بودن رشته (طول برابر با صفر).
  * `-n "$str"`: بررسی پر بودن رشته (طول بزرگتر از صفر).
  * `"$str1" = "$str2"`: بررسی تساوی دو رشته متنی.
* **بررسی اعداد صحیح (Integer Comparisons):**
  * `-eq` (برابر)، `-ne` (نابرابر)، `-lt` (کوچک‌تر)، `-gt` (بزرگ‌تر)، `-le` (کوچک‌تر یا مساوی)، `-ge` (بزرگ‌تر یا مساوی).

---

### 🔁 ۴. حلقه‌ها و تکرار (Loops: `for` & `while`)
* **حلقه `for`:** برای پیمایش یک لیست مشخص یا خروجی چندین فایل:
  ```bash
  for user in alice bob charlie; do ... done

```

* **حلقه `while`:** اجرای تسک‌ها تا زمانی که یک شرط خاص برقرار باشد:
```bash
while [ condition ]; do ... done

```



---

## ⚡ PART 2: THE ONE-CLICK ALL COMMANDS CODEBOX (کادر یکپارچه دستورات)

تمام اسکریپت‌های کاربردی، بررسی شرایط، کدهای خروج، حلقه‌ها و ساخت فایل‌های اجرایی به صورت یکجا در کادر زیر قرار دارند:

```bash
# ==============================================================================
# 🛠️ بخش اول: ساختار پایه، مجوزدهی و تست کدهای خروج (FOUNDATIONS & EXIT CODES)
# ==============================================================================

# ایجاد یک اسکریپت پایه با خط شه‌بنگ و مجوز اجرایی
cat << 'EOF' > /tmp/sysinfo.sh
#!/bin/bash
# اسکریپت جمع‌آوری اطلاعات اولیه سرور
HOSTNAME=$(hostname)
UPTIME=$(uptime -p)

echo "Server Hostname: $HOSTNAME"
echo "Server Uptime: $UPTIME"
exit 0
EOF

# دادن مجوز اجرا به فایل
chmod +x /tmp/sysinfo.sh

# اجرای اسکریپت در مسیر جاری
/tmp/sysinfo.sh

# بررسی کد خروج آخرین دستور اجرا شده (0 = Success)
echo $?


# ==============================================================================
# ⚖️ بخش دوم: ساختارهای شرطی و اعتبارسنجی ورودی (CONDITIONAL STATEMENTS)
# ==============================================================================

# اسکریپت بررسی وجود یک کاربر و اعتبارسنجی آرگومان خط فرمان
cat << 'EOF' > /tmp/check_user.sh
#!/bin/bash

# بررسی اینکه آیا حداقل یک ورودی وارد شده است یا خیر
if [ $# -lt 1 ]; then
    echo "Usage: $0 <username>"
    exit 1
fi

USER_NAME="$1"

# بررسی وجود کاربر در پایگاه‌داده passwd
if id "$USER_NAME" &>/dev/null; then
    echo "User $USER_NAME exists on this system."
    exit 0
else
    echo "User $USER_NAME does not exist!"
    exit 2
fi
EOF

chmod +x /tmp/check_user.sh


# ==============================================================================
# 📁 بخش سوم: بررسی فایل‌ها و تست‌های سیستمی (FILE & DIRECTORY TESTS)
# ==============================================================================

# اسکریپت اعتبارسنجی دایرکتوری بک‌آپ و فایل لاگ
cat << 'EOF' > /tmp/check_backup.sh
#!/bin/bash

BACKUP_DIR="/backup"
LOG_FILE="/var/log/backup.log"

# بررسی وجود دایرکتوری؛ در صورت عدم وجود، ایجاد می‌شود
if [ ! -d "$BACKUP_DIR" ]; then
    echo "Backup directory missing. Creating $BACKUP_DIR..."
    mkdir -p "$BACKUP_DIR"
fi

# بررسی وجود فایل گزارش
if [ -f "$LOG_FILE" ]; then
    echo "Log file exists and has size: $(ls -lh "$LOG_FILE" | awk '{print $5}')"
else
    touch "$LOG_FILE"
    echo "Log file created."
fi
EOF

chmod +x /tmp/check_backup.sh


# ==============================================================================
# 🔁 بخش چهارم: حلقه‌های FOR و WHILE (ITERATION & AUTOMATION)
# ==============================================================================

# ساخت چندین حساب کاربری به صورت اتوماتیک با حلقه FOR
cat << 'EOF' > /tmp/create_users.sh
#!/bin/bash

USERS_LIST="user1 user2 user3"

for USER in $USERS_LIST; do
    if id "$USER" &>/dev/null; then
        echo "Skipping: $USER already exists."
    else
        useradd "$USER"
        echo "RedHat123" | passwd --stdin "$USER" &>/dev/null
        echo "Created user: $USER"
    fi
done
EOF

chmod +x /tmp/create_users.sh

# خواندن فایل خط به خط با استفاده از حلقه WHILE
cat << 'EOF' > /tmp/read_hosts.sh
#!/bin/bash

FILE_PATH="/etc/hosts"

if [ -f "$FILE_PATH" ]; then
    while IFS= read -r line; do
        echo "HOST LINE: $line"
    done < "$FILE_PATH"
fi
EOF

chmod +x /tmp/read_hosts.sh

```

---

## ⚡ PART 3: QUICK REFERENCE INDEX (چیت‌شیت جیبی و سریع)

| ساختار / متغیر | عملکرد کلیدی |
| --- | --- |
| `#!/bin/bash` | شه‌بنگ؛ تعیین مفسر Bash برای اجرای اسکریپت |
| `$#` | شمارش تعداد کل ورودی‌های ارسال‌شده به اسکریپت |
| `$1, $2, ...` | فراخوانی آرگومان‌های اول، دوم و ... دریافتی از خط فرمان |
| `$?` | کد خروج آخرین پردازش (`0` موفقیت، ارقام دیگر نشان‌دهنده شکست) |
| `[ -d "$dir" ]` | شرط: آیا مسیر داده‌شده یک دایرکتوری واقعی است؟ |
| `[ -f "$file" ]` | شرط: آیا مسیر داده‌شده یک فایل عادی است؟ |
| `[ -z "$str" ]` | شرط: آیا متغیر متنی کاملاً خالی است؟ |
| `[ $a -eq $b ]` | مقایسه عددی: آیا دو عدد با یکدیگر برابر هستند؟ |
| `$(command)` | جایگزینی خروجی دستور و تخصیص مستقیم آن به یک متغیر |
| `chmod +x file.sh` | فعال‌سازی مجوز اجرا برای تبدیل فایل متنی به اسکریپت |

```

```