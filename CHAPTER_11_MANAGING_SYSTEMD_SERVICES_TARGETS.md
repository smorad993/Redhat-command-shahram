# 🏛️ CHAPTER 11: MANAGING SYSTEMD SERVICES & TARGETS

این فایل یک مرجع جامع و یکپارچه از مدیریت سرویس‌ها، کنترل وضعیت استارت‌آپ بوت، مدیریت تارگت‌ها (Targets) و ساخت فایل یونیت سفارشی در RHEL 9 است.

---

## 📄 PART 1: THE MANUAL (کتابچه مفاهیم و معماری SYSTEMD)

### 🧩 ۱. واحدها یا یونیت‌ها (Systemd Units)
سیستم‌دی مغز متفکر و اولین پروسسی است که در لینوکس بالا می‌آید (PID 1). این سیستم همه چیز را در قالب فایل‌های کانفیگ به نام **Unit** می‌بیند. رایج‌ترین پسوندهای یونیت عبارتند از:
* **`.service`:** مربوط به برنامه‌ها و دیمون‌های پس‌زمینه سرور (مثل `sshd`, `httpd`).
* **`.target`:** مجموعه‌ای از یونیت‌ها که یک وضعیت خاص از سیستم را تعریف می‌کنند (مشابه ران‌لول‌های قدیمی لینوکس).
* **`.timer`:** برای زمان‌بندی کارها (که در فصل ۱۲ خواندیم).

---

### 🎛️ ۲. تفاوت تئوری اقدامات لحظه‌ای (Live) و دائمی (Boot)
یکی از مفاهیم فوق‌العاده کنکوری، تفکیک رفتارهای دستور `systemctl` است:
* **اقدامات لحظه‌ای (`start`, `stop`, `restart`):** سرویس را در همین ثانیه کنترل می‌کنند. با ریبوت شدن سرور، اثر این دستورات می‌پرد!
* **اقدامات دائمی (`enable`, `disable`):** کاری به روشن یا خاموش بودن سرویس در این لحظه ندارند؛ بلکه با ساخت یا حذف یک لینک سمبولیک (Symbolic Link) در پوشه‌های سیستم‌دی، تعیین می‌کنند که آیا سرویس در **بوت بعدی سرور** خودکار روشن شود یا خیر.

---

### 🎯 ۳. مدیریت وضعیت‌های سیستم (System Targets)
تارگت‌ها مشخص می‌کنند لینوکس چطور بوت شود. در آزمون RHCSA باید این سه تارگت اصلی را مثل کف دست بشناسید:
* **`multi-user.target`:** محیط متنی پیش‌فرض سرورها (بدون گرافیک، شبکه فعال، حالت چندکاربره).
* **`graphical.target`:** محیط کاملاً گرافیکی (GUI). تمام سرویس‌های مالتی‌یوزر را هم درون خودش دارد.
* **`rescue.target`:** محیط اضطراری و تک‌کاربره برای عیب‌یابی سرور (زمانی که سرور خراب شده و بالا نمی‌آید).

---

### 🏗️ ۴. آناتومی یک یونیت سفارشی (ساختار سناریوی توکن شما)
وقتی می‌خواهید یک سرویس جدید (مثل برنامه‌های دیسکاوری) به لینوکس معرفی کنید، فایل کانفیگ آن شامل ۳ بخش کلیدی است:
1. **`[Unit]`:** اطلاعات کلی و وابستگی‌ها (مثلاً سرویس من باید حتماً بعد از وصل شدن شبکه [`After=network.target`] استارت شود).
2. **`[Service]`:** قلب تپنده کانفیگ! آدرس دقیق فایل اجرایی (`ExecStart`)، اسکریپت‌های پیش‌نیاز (`ExecStartPre`) و فایلی که متغیرهای محیطی در آن است (`EnvironmentFile`) در اینجا نوشته می‌شود.
3. **`[Install]`:** مشخص می‌کند این سرویس زیرمجموعه کدام تارگت اصلی سرور است تا موقع enable کردن زنجیر شود (معمولاً `WantedBy=multi-user.target`).

---
---

## ⚡ PART 2: THE ONE-CLICK ALL COMMANDS CODEBOX (کادر یکپارچه دستورات)

تمام دستورات مدیریت سرویس‌ها، وضعیت بوت، سوییچ بین تارگت‌ها و کدهای مهندسی فایل یونیت یکجا در این کادر برای کپی راحت جمع شده‌اند:

```bash
# ==============================================================================
# 🚀 بخش اول: اقدامات لحظه‌ای و زنده سرویس‌ها (INSTANT ACTIONS)
# ==============================================================================

# روشن کردن فوری یک سرویس (تغییری در بوت بعدی ایجاد نمی‌کند)
sudo systemctl start httpd

# خاموش کردن فوری و آنی سرویس
sudo systemctl stop httpd

# ریستارت کامل (ابتدا سرویس را می‌کشد و دوباره از نو متولد می‌کند)
sudo systemctl restart httpd

# ریلود کردن کانفیگ برنامه بدون قطع شدن اتصال کاربران (در صورت ساپورت برنامه)
sudo systemctl reload httpd

# نمایش وضعیت زنده، میزان مصرف رم، PID اصلی و آخرین لاگ‌های یک سرویس
systemctl status httpd

# !!! ترفند عیب‌یابی !!! نمایش وضعیت با خطوط کامل و بدون قطع شدن کلمات طولانی
systemctl status httpd -l


# ==============================================================================
# 📌 بخش دوم: مدیریت پایداری و استارت‌آپ بوت سرور (BOOT PERSISTENCE)
# ==============================================================================

# زنجیر کردن سرویس به استارت‌آپ بوت سرور (روشن شدن خودکار پس از ریبوت)
sudo systemctl enable httpd

# حذف سرویس از استارت‌آپ بوت (پس از ریبوت سرور، سرویس خاموش می‌ماند)
sudo systemctl disable httpd

# ترکیب مقتدرانه: هم سرویس را همین الان روشن کن و هم به استارت‌آپ بوت اضافه کن
sudo systemctl enable --now httpd

# چک کردن اینکه آیا یک سرویس برای بوت بعدی فعال است یا خیر (خروجی: enabled/disabled)
systemctl is-enabled httpd

# چک کردن اینکه آیا سرویس در همین ثانیه در رم سرور روشن است (خروجی: active/inactive)
systemctl is-active httpd

# مسدودسازی کامل (Mask)! جلوی استارت شدن سرویس را حتی توسط سرویس‌های دیگر می‌گیرد
sudo systemctl mask httpd

# خارج کردن سرویس از حالت مسدودسازی
sudo systemctl unmask httpd


# ==============================================================================
# 🎯 بخش سوم: مدیریت تارگت‌ها و وضعیت‌های بوت (SYSTEM TARGETS)
# ==============================================================================

# نمایش تارگت پیش‌فرض فعلی سرور (لینوکس در چه حالتی بوت می‌شود؟)
systemctl get-default

# تغییر دائم تارگت پیش‌فرض سیستم به حالت متنی سروری (بدون گرافیک)
sudo systemctl set-default multi-user.target

# تغییر دائم تارگت پیش‌فرض سیستم به حالت گرافیکی
sudo systemctl set-default graphical.target

# سوییچ کردن آنی و زنده سرور به یک تارگت دیگر بدون نیاز به ریبوت
sudo systemctl isolate multi-user.target


# ==============================================================================
# 🛠️ بخش چهارم: ساخت فایل یونیت اختصاصی و لود مجدد (UNIT CONFIGURATION)
# ==============================================================================

# نمونه ساختار فایل یونیت سفارشی برای سرویس‌های دیسکاوری شما:
# فایل را در این مسیر بسازید: sudo nano /etc/systemd/system/discovery-nrd.service
# ------------------------------------------------------------------------------
# [Unit]
# Description=Discovery NRD Core Service
# After=network.target
#
# [Service]
# Type=simple
# ExecStartPre=/usr/local/bin/get-token.sh
# EnvironmentFile=/run/nrd-token.env
# ExecStart=/usr/local/bin/discovery-nrd-ccp
# Restart=on-failure
# TimeoutStartSec=180
#
# [Install]
# WantedBy=multi-user.target
# ------------------------------------------------------------------------------

# !!! فوق حیاتی !!! هر زمان فایل کانفیگی را دستکاری یا خلق کردی، این را بزن تا سیستم‌دی بفهمد
sudo systemctl daemon-reload

# نمایش لیست تمام سرویس‌های فعال سرور که کلمه discovery در نامشان است
systemctl list-units "discovery-*"

# شکار سریع تمام سرویس‌های کل سرور که کرش کرده‌اند و در وضعیت خراب هستند
systemctl --failed