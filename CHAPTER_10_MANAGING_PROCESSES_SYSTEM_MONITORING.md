# ⚙️ CHAPTER 11: MANAGING PROCESSES & SYSTEM MONITORING

این فایل یک مرجع جامع و یکپارچه از مانیتورینگ سیستم، مدیریت پروسس‌ها، ارسال سیگنال‌ها و تنظیم اولویت اجرا (Nice) در RHEL 9 است.

---

## 📄 PART 1: THE MANUAL (کتابچه مفاهیم و وضعیت‌شناسی پروسس‌ها)

### 📊 ۱. حالت‌های پنج‌گانه یک پروسس (Process States)
هر برنامه‌ای که در لینوکس اجرا می‌شود (Process)، در طول چرخه حیات خود در یکی از حالت‌های زیر قرار می‌گیرد که در خروجی دستوراتی مثل `ps` با این حروف اختصاری نمایش داده می‌شوند:
* **`R` (Running / Runnable):** پروسس در حال حاضر یا در حال اجرا روی CPU است یا در صف انتظارِ آماده‌به‌کار قرار دارد.
* **`S` (Interruptible Sleep):** پروسس در خواب است و منتظر یک رویداد یا ورودی (مثل کلیک کاربر یا رسیدن دیتا) می‌ماند. با سیگنال‌ها بیدار می‌شود.
* **`D` (Uninterruptible Sleep):** خواب عمیق و غیرقابل بیدار کردن! معمولاً پروسس منتظر یک عملیات سخت‌افزاری یا I/O دیسک است و تا کارش تمام نشود به هیچ سیگنالی (حتی Kill) پاسخ نمی‌دهد.
* **`T` (Stopped):** پروسس موقتاً متوقف یا فریز شده است (مثلاً با زدن کلیدهای Ctrl + Z توسط ادمین).
* **`Z` (Zombie):** پروسس مرده است! کارش تمام شده اما به دلیل اینکه پروسس پدری (Parent) وضعیت آن را بررسی نکرده، هنوز ردپای آن در جدول پروسس‌های رم باقی مانده است. زامبی‌ها CPU مصرف نمی‌کنند اما جدول PIDها را اشغال می‌کنند.

---

### 📡 ۲. سیگنال‌های استراتژیک مدیریت پروسس (Signals)
برای صحبت کردن و فرمان دادن به پروسس‌ها، لینوکس از مکانیزمی به نام سیگنال استفاده می‌کند. از بین ده‌ها سیگنال موجود، این ۴ مورد فوق‌العاده کنکوری و عملیاتی هستند:
* **`SIGTERM` (Signal 15):** سیگنال پیش‌فرض و مودبانه! به پروسس می‌گوید «لطفاً کار خودت را جمع کن، فایل‌هایت را ببند و به صورت امن خارج شو».
* **`SIGKILL` (Signal 9):** سیگنال خشن و بی‌رحم! بدون معطلی و بدون اجازه دادن به پروسس برای بستن فایل‌ها، آن را فوراً در سطح رم متلاشی و نابود می‌کند. زمانی استفاده می‌شود که برنامه قفل کرده و به حالت فریز درآمده است.
* **`SIGHUP` (Signal 1):** سیگنال بازخوانی. به پروسس دستور می‌دهد بدون اینکه کاملاً متوقف شود، فایل‌های تنظیمات (Configuration) خود را دوباره لود و بازخوانی کند (بسیار مفید برای وب‌سرورها).
* **`SIGSTOP` (Signal 19):** پروسس را بدون کشتن، در همان لحظه موقتاً فریز و متوقف می‌کند.

---

### 🏎️ ۳. مهندسی اولویت اجرای پروسس (Nice & Renice)
همه پروسس‌ها ارزش یکسانی برای CPU ندارند. لینوکس با استفاده از مفهومی به نام **Nice Value** اولویت پروسس‌ها را مشخص می‌کند:
* بازه عددی Nice بین **`20-` تا `19+`** است (کلاً ۴۰ پله اولویت).
* **عدد Nice پیش‌فرض:** هر برنامه‌ای به صورت عادی با عدد `0` شروع به کار می‌کند.
* **قانون معکوس اولویت:** هر چقدر عدد Nice **کمتر و منفی‌تر** باشد، پروسس آدم خودخواه‌تری است و اولویت بالاتر (High Priority) دارد و CPU بیشتری می‌قاپد! برعکس، هر چقدر عدد **بیشتر و مثبت‌تر** باشد، پروسس مهربان‌تر (Nice) است، اولویت پایین‌تری دارد و سهم CPU خود را به دیگران می‌دهد.
* 🔐 **نکته امنیتی:** کاربران عادی فقط می‌توانند عدد نایس برنامه‌های خودشان را مثبت‌تر کنند (اولویت را پایین بیاورند). فقط کاربر **root** دسترسی دارد که اولویت را منفی و بالاتر ببرد.

---
---

## ⚡ PART 2: THE ONE-CLICK ALL COMMANDS CODEBOX (کادر یکپارچه دستورات)

تمام دستورات ناوبری، مانیتورینگ زنده، پیدا کردن PIDها، ارسال سیگنال‌ها و تنظیمات Nice یکجا در این کادر برای کپی راحت جمع شده‌اند:

```bash
# ==============================================================================
# 👁️ بخش اول: مانیتورینگ و آنالیز وضعیت پروسس‌ها (PROCESS MONITORING)
# ==============================================================================

# ابزار زنده و تعاملی مانیتورینگ مصرف CPU و رم (خروج با کلید q)
top

# ترفندهای داخل محیط top:
# کلید P ➔ مرتب‌سازی پروسس‌ها بر اساس بیشترین مصرف CPU
# کلید M ➔ مرتب‌سازی پروسس‌ها بر اساس بیشترین مصرف حافظه (RAM)
# کلید k ➔ کشتن مستقیم یک پروسس با وارد کردن PID درون محیط top

# نمایش تمام پروسس‌های در حال اجرای سیستم با جزئیات کامل (User, PID, CPU, State)
ps aux

# پیدا کردن پروسس‌ها به صورت ساختار درختی برای دیدن پروسس‌های پدر و فرزند
ps fax

# فیلتر کردن و پیدا کردن سریع شماره پروسس (PID) یک برنامه خاص (مثال: sshd)
pgrep -l sshd

# نمایش پروسس‌های در حال اجرای یک کاربر خاص (مثال: کاربر student)
ps -u student


# ==============================================================================
# 📡 بخش دوم: شکار و ارسال سیگنال به پروسس‌ها (SENDING SIGNALS)
# ==============================================================================

# کشتن مودبانه یک پروسس با استفاده از شماره PID آن (سیگنال ۱۵ - پیش‌فرض)
kill 1234

# نابود کردن اجباری و آنی پروسس قفل شده با سیگنال معروف 9 (SIGKILL)
kill -9 1234

# دستور دادن به پروسس برای بازخوانی فایل تنظیمات بدون ریستارت شدن (SIGHUP)
kill -1 1234

# کشتن دسته جمعی پروسس‌ها بر اساس "نام برنامه" به جای PID (مثال: تمام پروسس‌های httpd)
sudo killall httpd

# کشتن پروسس‌ها بر اساس فیلترهای پیشرفته‌تر نام (پشتیبانی از پترن)
pkill -9 -u student httpd


# ==============================================================================
# 🏎️ بخش سوم: مدیریت اولویت اجرا و سهم پردازنده (NICE & RENICE)
# ==============================================================================

# ۱. استارت کردن یک برنامه جدید با یک اولویت اختصاصی و بالاتر (مثال: اولویت -5)
sudo nice -n -5 nginx

# ۲. استارت کردن یک برنامه با اولویت پایین‌تر تا سیستم کند نشود (مثال: اولویت +10)
nice -n 10 tar -czf backup.tar.gz /var/log

# ۳. تغییر اولویت یک پروسس که در حال حاضر در حال اجراست (مثال: تغییر نایس پروسس 4321 به -10)
sudo renice -n -10 -p 4321

# ۴. تغییر اولویت تمام پروسس‌های در حال اجرای یک کاربر خاص به صورت یکجا
sudo renice -n 5 -u student


# ==============================================================================
# ⏳ بخش چهارم: مدیریت پروسس‌ها در پس‌زمینه و پیش‌زمینه (JOB CONTROL)
# ==============================================================================

# اجرای یک دستور سنگین مستقیماً در پس‌زمینه (Background) با قرار دادن علامت & در انتها
tar -czf bigfile.tar.gz /data &

# مشاهده لیست کارهای پس‌زمینه فعال در ترمینال فعلی شما به همراه Job ID
jobs

# آوردن کار شماره ۱ از پس‌زمینه به پیش‌زمینه (Foreground) برای مانیتور عادی
fg %1

# فرستادن پروسس حال حاضر ترمینال به پس‌زمینه:
# ابتدا کلیدهای Ctrl + Z را بزنید تا پروسس متوقف (Stop) شود، سپس دستور زیر را بزنید:
bg %1