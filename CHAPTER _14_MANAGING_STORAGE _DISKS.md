# 💾 CHAPTER 14: MANAGING STORAGE & DISKS

این فایل یک مرجع جامع و یکپارچه از مدیریت دیسک‌ها، پارتیشن‌بندی، فایل‌سیستم‌ها و حافظه Swap در RHEL 9 است.

---

## 📄 PART 1: THE MANUAL (کتابچه مفاهیم و نقشه راه استوریج)

### 🗺️ ۱. ساختار نام‌گذاری دیسک‌ها در لینوکس
لینوکس تمام قطعات سخت‌افزاری را به عنوان یک فایل در پوشه `/dev/` می‌بیند. دیسک‌ها بر اساس نوع کنترلرشان نام‌گذاری می‌شوند:
* **`/dev/sdX`:** دیسک‌های SATA، SCSI یا حافظه‌های USB (مانند `sda`, `sdb`). پارتیشن‌های آن با عدد مشخص می‌شوند (مانند `sda1`, `sda2`).
* **`/dev/vdaX`:** دیسک‌های مجازی در محیط‌های KVM/Qemu (مخفف Virtual Disk).
* **`/dev/nvmeXnXpX`:** دیسک‌های فوق سریع مدرن NVMe (مانند `nvme0n1`). پارتیشن آن به صورت `nvme0n1p1` نشان داده می‌شود.

---

### 🎛️ ۲. تفاوت MBR (fdisk) و GPT (gdisk)
برای تقسیم‌بندی یک دیسک خام، دو نوع جدول پارتیشن (Partition Table) داریم:
* **MBR (Master Boot Record):** روش قدیمی. حداکثر تا ۲ ترابایت دیسک را پشتیبانی می‌کند و فقط اجازه ساخت ۴ پارتیشن اصلی (Primary) را می‌دهد (برای بیشتر شدن باید پارتیشن Extended و Logical ساخت). ابزار مدیریت آن `fdisk` است.
* **GPT (GUID Partition Table):** روش مدرن و پیش‌فرض RHEL 9. دیسک‌های بزرگتر از ۲ ترابایت را ساپورت می‌کند و اجازه ساخت تا ۱۲۸ پارتیشن را بدون محدودیت می‌دهد. ابزار مدیریت آن `gdisk` است.

⚠️ **نکته حیاتی (اطلاع به هسته):** وقتی جدول پارتیشن دیسک را تغییر می‌دهید، هسته لینوکس (Kernel) تغییرات را زنده نمی‌بیند. برای اینکه سرور را ریبوت نکنید، حتماً باید دستور `partprobe` را بزنید تا جدول پارتیشن در رم بازخوانی شود.

---

### 🧱 ۳. ساخت فایل‌سیستم (Formatting) و معماری XFS
یک پارتیشن خام مثل زمین بدون خط‌کشی است. با فرمت کردن، ساختار ذخیره‌سازی را روی آن پیاده می‌کنیم:
* **XFS:** فایل‌سیستم پیش‌فرض و فوق‌العاده قدرتمند در RHEL 9. برای هارد‌های با حجم بالا و سرورهای سازمانی بهینه شده است. (ابزار: `mkfs.xfs`).
* **Ext4:** فایل‌سیستم سنتی و پایداری که هنوز در بسیاری از پروژه‌ها استفاده می‌شود. (ابزار: `mkfs.ext4`).

---

### 🔗 ۴. کالبدشکافی فایل استراتژیک `/etc/fstab`
برای اینکه پس از ریبوت شدن سرور، دیسک‌ها خود به خود بالا بیایند (Mount دائم)، باید مشخصات آن‌ها را در فایل `/etc/fstab` بنویسید. این فایل شامل ۶ ستون حیاتی است:

```ini
[Device/UUID]  [Mount_Point]  [File_System]  [Options]  [Dump]  [FSCK]

# ==============================================================================
# 🔍 بخش اول: بررسی و شناسایی دیسک‌های سیستم (DISK DISCOVERY)
# ==============================================================================

# نمایش تمام دیسک‌ها و پارتیشن‌ها به صورت ساختار درختی و شیک
lsblk

# نمایش لیست تمام دیسک‌ها به همراه جدول پارتیشن آن‌ها
sudo fdisk -l

# پیدا کردن UUID (آیدی منحصربه‌فرد) تمام پارتیشن‌ها برای استفاده در fstab
sudo blkid


# ==============================================================================
# 🛠️ بخش دوم: پارتیشن‌بندی با GDISK (مدرن - GPT)
# ==============================================================================

# ورود به محیط تعاملی پارتیشن‌بندی دیسک دوم (مثال: sdb)
# ترفند کلیدها در محیط gdisk:
# n ➔ ساخت پارتیشن جدید
# p ➔ نمایش پارتیشن‌های ساخته شده
# w ➔ ذخیره تغییرات و خروج
# q ➔ خروج بدون ذخیره‌سازی
sudo gdisk /dev/sdb

# !!! حیاتی !!! اطلاع‌رسانی آنی به هسته لینوکس بدون نیاز به ریبوت سرور
sudo partprobe


# ==============================================================================
# 🧱 بخش سوم: ساخت فایل‌سیستم و مانت کردن (FORMAT & MOUNT)
# ==============================================================================

# فرمت کردن پارتیشن تازه ساخته شده با فایل‌سیستم پیش‌فرض XFS
sudo mkfs.xfs /dev/sdb1

# فرمت کردن با فایل‌سیستم Ext4 (در صورت نیاز سناریو)
sudo mkfs.ext4 /dev/sdb2

# ساخت پوشه هدف برای باز کردن (Mount) دیسک
sudo mkdir -p /mnt/shahdata

# مانت کردن موقت دیسک در پوشه ساخته شده
sudo mount /dev/sdb1 /mnt/shahdata

# بررسی حجم دیسک‌های مانت شده سیستم به صورت خوانا (Human-Readable)
df -h

# قطع اتصال و آن‌مانت کردن دیسک
sudo umount /mnt/shahdata


# ==============================================================================
# 🔗 بخش چهارم: دائمی کردن مانت دیسک پس از ریبوت (FSTAB)
# ==============================================================================

# خط نمونه استاندارد برای اضافه کردن به ته فایل /etc/fstab:
# ابتدا با دستور sudo blkid آیدی UUID دیسک sdb1 را بردارید، سپس فایل را باز کنید:
# sudo nano /etc/fstab
# نمونه خط تزریقی:
# UUID=1234-abcd-5678    /mnt/shahdata    xfs    defaults    0    2

# !!! فوق حیاتی !!! تست صحت فایل fstab قبل از ریبوت سرور (اگه ارور نداد یعنی کاملاً امنه)
sudo mount -a


# ==============================================================================
# 🐏 بخش پنجم: ایجاد و فعال‌سازی حافظه مجازی (SWAP SPACE)
# ==============================================================================

# فرض کنید پارتیشن sdb2 را در gdisk با کد تایپ 8200 (سواپ) ساخته‌اید:

# ۱. فرمت کردن و تغییر روح پارتیشن به حافظه Swap
sudo mkswap /dev/sdb2

# ۲. روشن کردن و فعال‌سازی آنی حافظه Swap در سیستم
sudo swapon /dev/sdb2

# ۳. بررسی وضعیت و حجم حافظه‌های Swap فعال در سرور
swapon --show
free -m

# خط نمونه برای دائمی کردن Swap در فایل /etc/fstab (تزریق به ته فایل):
# UUID=uuid_پارتیشن_سواپ    none    swap    defaults    0    0

# خاموش کردن و غیرفعال‌سازی حافظه سواپ
sudo swapoff /dev/sdb2