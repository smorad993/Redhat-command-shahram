# 📕 CHAPTER 13: CONFIGURING & MANAGING LINUX LOGS

این فایل شامل دو بخش است:
1. **The Manual:** مفاهیم عمیق معماری، ساختارشناسی و فایل‌های لاگ در RHEL 9.
2. **The Cheat Sheet:** زرادخانه عملیاتی فرمان‌ها، فیلترهای جادویی و سناریوهای عیب‌یابی.

---

## 📄 PART 1: THE MANUAL (راهنمای جامع مفاهیم)

### 🧩 ۱. معماری سه‌گانه ثبت لاگ در RHEL 9
سیستم‌عامل لینوکس برای مدیریت و ثبت رویدادها از سه ساختار کاملاً مجزا استفاده می‌کند:
* **سرویس `systemd-journald`:** راهکار مدرن و پیش‌فرض سیستم. لاگ‌ها را زنده از هسته (Kernel)، فرآیند بوت و تمام سرویس‌ها جمع کرده و به صورت **باینری (Binary)** ذخیره می‌کند. این لاگ‌ها به صورت پیش‌فرض موقتی هستند و با ریبوت سیستم پاک می‌شوند چون در رم (`/run/log/journal`) نگهداری می‌شوند.
* **سرویس `rsyslogd`:** سیستم سنتی اما ارتقایافته لینوکس. یک کپی از لاگ‌های `journald` را تحویل می‌گیرد و آن‌ها را به صورت **متن صاف (Plain Text) و دائمی** در پوشه `/var/log` ذخیره می‌کند تا بعد از ریبوت باقی بمانند. این سرویس قابلیت فیلترنویسی و مدیریت متمرکز لاگ‌ها را دارد.
* **نوشتن مستقیم (Direct Write):** برخی برنامه‌ها و سرویس‌های بزرگ (مثل Apache یا Samba) مستقیماً خودشان فایل لاگ را در پوشه اختصاصی خود می‌نویسند و سیستم rsyslog را دور می‌زنند. کتاب این روش را برای برنامه‌های عمومی توصیه نمی‌کند.

---

### 🗺️ ۲. نقشه‌ی فایل‌های لاگ متنی در `/var/log`
هر زمان سیستم یا برنامه‌ای با بحران مواجه شد، بر اساس جدول استاندارد زیر باید به سراغ فایل متنی مربوطه در مسیر `/var/log/` بروید:

| مسیر فایل لاگ | محتوای گزارش شده و کاربرد اصلی |
| :--- | :--- |
| **`/var/log/messages`** | بایگانی عمومی؛ بیشترین گزارشات کلی سیستم و سرویس‌ها اینجاست. |
| **`/var/log/secure`** | امنیت و احراز هویت؛ گزارش ورودها، خطا در پسورد و دستورات مربوط به SSH. |
| **`/var/log/dmesg`** | گزارشات سخت‌افزاری و پیام‌های مستقیم هسته لینوکس (Kernel). |
| **`/var/log/boot.log`** | گزارشات مربوط به سرویس‌ها در لحظه استارت‌آپ و روشن شدن سرور. |
| **`/var/log/audit/audit.log`** | گزارشات سیستم حساب‌رسی امنیتی عمیق و لاگ‌های حیاتی **SELinux**. |
| **`/var/log/maillog`** | گزارش‌های مربوط به ارسال، دریافت و خطاهای سرور ایمیل سیستم. |

---

### 🔬 ۳. آناتومی استاندارد خطوط لاگ
هر خط لاگی که در لینوکس باز می‌کنید (به ویژه در فایل `/var/log/messages`) از ۴ بخش کلیدی از چپ به راست تشکیل شده است:
1. **Date and Time (برچسب زمان):** تاریخ و زمان دقیق رویداد به صورت ساعت ۲۴ ساعته (نظامی) برای فیلتر کردن راحت‌تر.
2. **Host (نام هاست):** نام سرور یا کامپیوتری که لاگ روی آن تولید شده (بسیار مهم در سیستم‌های لاگ متمرکز شبکه).
3. **Service/Process & PID:** نام برنامه یا سرویس تولیدکننده لاگ به همراه شماره پروسس اختصاصی آن در کروشه (مانند `sshd[1055]`).
4. **Message Content:** متن، شرح واقعه و جزئیات دقیق اتفاق رخ داده.

---

### 🎯 ۴. ساختار مثلث قوانین تفکیک در rsyslog
برای نوشتن هر قانونی در فایل `/etc/rsyslog.conf` یا پوشه ماژولار `/etc/rsyslog.d/` از فرمول استاندارد زیر استفاده می‌شود:

```ini
facility.priority    destination
# شنود زنده و آنی انتهای فایل لاگ عمومی سیستم (خروج با Ctrl+C)
tail -f /var/log/messages

# نمایش ۱۰ خط آخر فایل امنیت سیستم برای چک کردن ورود کاربران
tail -10 /var/log/secure

# مانیتورینگ زنده کل دیتابیس باینری ژورنال سیستم
journalctl -f

# باز کردن ژورنال و پرش مستقیم به انتهای لاگ‌ها (آخرین رویدادها)
# نکته: پس از اجرای دستور، کلید حروف بزرگ G (یا همان Shift + G) را بزنید
journalctl
# شنود زنده و آنی انتهای فایل لاگ عمومی سیستم (خروج با Ctrl+C)
tail -f /var/log/messages

# نمایش ۱۰ خط آخر فایل امنیت سیستم برای چک کردن ورود کاربران
tail -10 /var/log/secure

# مانیتورینگ زنده کل دیتابیس باینری ژورنال سیستم
journalctl -f

# باز کردن ژورنال و پرش مستقیم به انتهای لاگ‌ها (آخرین رویدادها)
# نکته: پس از اجرای دستور، کلید حروف بزرگ G (یا همان Shift + G) را بزنید
journalctl
# گام‌های فیزیکی دائمی کردن ژورنال روی هارد با تنظیمات دقیق دسترسی (SGID)
sudo mkdir -p /var/log/journal
sudo chown root:systemd-journal /var/log/journal
sudo chmod 2755 /var/log/journal
sudo systemctl restart systemd-journal-flush

# مشاهده میزان دقیق حجمی که لاگ‌های ژورنال روی هارد اشغال کرده‌اند
journalctl --disk-usage

# خانه تکانی دستی هارد: کاهش حجم کل لاگ‌های ژورنال به حداکثر ۲ گیگابایت
sudo journalctl --vacuum-size=2G

# خانه تکانی دستی هارد: حذف تمام لاگ‌های ژورنال قدیمی‌تر از ۲ هفته
sudo journalctl --vacuum-time=2weeks

# گام‌های فیزیکی دائمی کردن ژورنال روی هارد با تنظیمات دقیق دسترسی (SGID)
sudo mkdir -p /var/log/journal
sudo chown root:systemd-journal /var/log/journal
sudo chmod 2755 /var/log/journal
sudo systemctl restart systemd-journal-flush

# مشاهده میزان دقیق حجمی که لاگ‌های ژورنال روی هارد اشغال کرده‌اند
journalctl --disk-usage

# خانه تکانی دستی هارد: کاهش حجم کل لاگ‌های ژورنال به حداکثر ۲ گیگابایت
sudo journalctl --vacuum-size=2G

# خانه تکانی دستی هارد: حذف تمام لاگ‌های ژورنال قدیمی‌تر از ۲ هفته
sudo journalctl --vacuum-time=2weeks

# گام‌های فیزیکی دائمی کردن ژورنال روی هارد با تنظیمات دقیق دسترسی (SGID)
sudo mkdir -p /var/log/journal
sudo chown root:systemd-journal /var/log/journal
sudo chmod 2755 /var/log/journal
sudo systemctl restart systemd-journal-flush

# مشاهده میزان دقیق حجمی که لاگ‌های ژورنال روی هارد اشغال کرده‌اند
journalctl --disk-usage

# خانه تکانی دستی هارد: کاهش حجم کل لاگ‌های ژورنال به حداکثر ۲ گیگابایت
sudo journalctl --vacuum-size=2G

# خانه تکانی دستی هارد: حذف تمام لاگ‌های ژورنال قدیمی‌تر از ۲ هفته
sudo journalctl --vacuum-time=2weeks

# گام‌های فیزیکی دائمی کردن ژورنال روی هارد با تنظیمات دقیق دسترسی (SGID)
sudo mkdir -p /var/log/journal
sudo chown root:systemd-journal /var/log/journal
sudo chmod 2755 /var/log/journal
sudo systemctl restart systemd-journal-flush

# مشاهده میزان دقیق حجمی که لاگ‌های ژورنال روی هارد اشغال کرده‌اند
journalctl --disk-usage

# خانه تکانی دستی هارد: کاهش حجم کل لاگ‌های ژورنال به حداکثر ۲ گیگابایت
sudo journalctl --vacuum-size=2G

# خانه تکانی دستی هارد: حذف تمام لاگ‌های ژورنال قدیمی‌تر از ۲ هفته
sudo journalctl --vacuum-time=2weeks