# ⏰ CHAPTER 12: SCHEDULING TASKS & TIMERS

این فایل یک مرجع جامع و یکپارچه از زمان‌بندی کارهای خودکار، مدیریت کرون‌جاب‌ها (Cron Jobs) و سیستم تایمرهای مدرن در RHEL 9 است.

---

## 📄 PART 1: THE MANUAL (کتابچه مفاهیم و ساختارشناسی زمان‌بندی)

### 📊 ۱. ساختار پنج‌ستاره کرون (Cron جدول زمانی)
ابزار `cron` قدیمی‌ترین و رایج‌ترین ابزار برای اجرای کارهای تکراری (مثلاً هر شب ساعت ۱۲) در لینوکس است. هر خط در فایل کرون‌تاب (`crontab`) شامل ۵ ستون زمان‌بندی به همراه آدرس اسکریپت یا دستور است.

فرمول ۵ ستاره از چپ به راست به این صورت مهندسی می‌شود:
```ini
* * * * * /path/to/command
▲      ▲      ▲      ▲      ▲
│      │      │      │      └─── روزهای هفته (0 تا 6، که 0 یا 7 یکشنبه است)
│      │      │      └────────── ماه‌های سال (1 تا 12)
│      │      └───────────────── روزهای ماه (1 تا 31)
│      └──────────────────────── ساعت (0 تا 23)
└─────────────────────────────── دقیقه (0 تا 59)
# ==============================================================================
# ⏰ بخش اول: مدیریت کرون‌جاب‌های شخصی (USER CRONTAB)
# ==============================================================================

# ویرایش و باز کردن فایل کرون‌جاب شخصی کاربر فعلی (با ادیتور پیش‌فرض مثل vim)
crontab -e

# خطوط نمونه برای تزریق به محیط crontab -e:
# ------------------------------------------------------------------------------
# */5 * * * * /path/to/script.sh            ➔ اجرای اسکریپت هر ۵ دقیقه یک‌بار
# 0 2 * * 1-5 /path/to/backup.sh            ➔ هر دوشنبه تا جمعه راس ساعت ۲:۰۰ بامداد
# 30 23 15 * * /path/to/report.sh           ➔ روز پانزدهم هر ماه ساعت ۲۳:۳۰ شب
# ------------------------------------------------------------------------------

# نمایش و لیست کردن تمام کارهای زمان‌بندی شده کاربر فعلی
crontab -l

# حذف کامل و پاکسازی تمام کرون‌جاب‌های کاربر فعلی
crontab -r

# مدیریت کرون‌جاب یک کاربر دیگر (فقط با دسترسی root یا sudo)
sudo crontab -u student -l
sudo crontab -u student -e


# ==============================================================================
# 🔓 بخش دوم: امنیت و فیلترینگ دسترسی به کرون (CRON SECURITY)
# ==============================================================================

# اگر می‌خواهید یک کاربر را مسدود کنید تا نتواند زمان‌بندی ایجاد کند:
# نام کاربر را داخل این فایل بنویسید:
sudo nano /etc/cron.deny

# اگر می‌خواهید فقط و فقط به کاربران خاصی اجازه استفاده از کرون را بدهید:
# نکته: ساخت این فایل باعث می‌شود بقیه کاربران خود به خود مسدود شوند!
sudo nano /etc/cron.allow


# ==============================================================================
# ⏳ بخش سوم: زمان‌بندی یک‌باره با ابزار AT (ONE-TIME TASKS)
# ==============================================================================

# برنامه ریزی یک کار برای ساعت ۴ عصر امروز (ورود به محیط تعاملی at)
# پس از تایپ دستورات، برای ذخیره و خروج کلید Ctrl + D را بزنید
at 16:00

# کارهایی که می‌توانید داخل محیط تعاملی at بنویسید (مثال):
# at> logger "At command executed!"
# at> <Ctrl+D>

# مثال‌های دیگر برای وارد کردن زمان در دستور at:
at 10:00 AM tomorrow
at now + 2 hours
at 2:30 PM July 15

# نمایش لیست کارهای یک‌باره در صف انتظار به همراه شماره Job آیدی
atq

# حذف یک کار مشخص از صف انتظار با استفاده از شماره آیدی آن (مثال: آیدی ۳)
atrm 3


# ==============================================================================
# ⚙️ بخش چهارم: مهندسی تایمرهای سیستمی (SYSTEMD TIMERS)
# ==============================================================================

# برای ساخت یک تایمر سفارشی (مثال: دیسکاوری)، به دو فایل در مسیر زیر نیاز داریم:

# گام ۱: ساخت فایل سرویس که مشخص می‌کند چه کاری انجام شود
# sudo nano /etc/systemd/system/discovery-cleanup.service
# [Unit]
# Description=Run discovery cleanup script
# [Service]
# ExecStart=/usr/local/bin/cleanup.sh

# گام ۲: ساخت فایل هم‌نام تایمر که مشخص می‌کند چه زمانی اجرا شود
# sudo nano /etc/systemd/system/discovery-cleanup.timer
# [Unit]
# Description=Run discovery cleanup every day
# [Timer]
# OnCalendar=*-*-* 03:00:00     ➔ یعنی هر روز ساعت ۳ صبح اجرا بشه
# Persistent=true               ➔ اگه سرور خاموش بود، بعد از روشن شدن فوراً اجراش کن
# [Install]
# WantedBy=timers.target

# گام ۳: لود کردن مجدد کانفیگ‌های سیستم‌دی پس از ساخت فایل‌ها
sudo systemctl daemon-reload

# گام ۴: فعال‌سازی و روشن کردن تایمر (نه سرویس!) برای استارت‌آپ بوت
sudo systemctl enable --now discovery-cleanup.timer

# گام ۵: بررسی وضعیت و لاگ‌های زنده تایمر ساخته شده
systemctl status discovery-cleanup.timer

# نمایش لیست تمام تایمرهای فعال در سرور به همراه زمان اجرای بعدی و قبلی
systemctl list-timers

# مانیتورینگ دقیق و عیب‌یابی خروجی تایمر از طریق ژورنال لینوکس
journalctl -u discovery-cleanup.service