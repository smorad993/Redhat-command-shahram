```markdown
# 🧠 CHAPTER 16: BASIC KERNEL MANAGEMENT

مرجع جامع و یکپارچه مدیریت هسته لینوکس (Kernel)، کار با ماژول‌های کرنل، تنظیم پارامترهای زمان اجرا (`sysctl`) و مدیریت بوت‌لودر GRUB2 در RHEL 9.

---

## 📄 PART 1: THE MANUAL (کتابچه مفاهیم و ساختارشناسی هسته لینوکس)

### 🧱 ۱. ساختار ماژولار هسته لینوکس (Loadable Kernel Modules - LKM)
کرنل لینوکس یک ساختار ماژولار دارد؛ یعنی برای پشتیبانی از یک سخت‌افزار یا قابلیت جدید، نیازی به کامپایل مجدد کل هسته نیست. ماژول‌ها کدهای کامپایل‌شده‌ای با پسوند `.ko` (Kernel Object) هستند که در مسیر زیر ذخیره می‌شوند:
`/lib/modules/$(uname -r)/`

* **`lsmod`:** جدول ماژول‌های لودشده فعلی در رم سرور را نشان می‌دهد.
* **`modinfo`:** شناسنامه، نویسنده، مجوز و پارامترهای ورودی یک ماژول را نمایش می‌دهد.
* **`modprobe`:** ابزار هوشمند لود یا آنلود کردن ماژول‌ها. تفاوت کلیدی آن با دستورات قدیمی مثل `insmod` این است که **وابستگی‌ها (Dependencies)** را بر اساس فایل نقشه `modules.dep` به صورت خودکار شناسایی و لود می‌کند.

---

### 🛡️ ۲. دائمی کردن و مسدودسازی ماژول‌ها (Persistence & Blacklisting)
* **لود خودکار در هنگام بوت:** اگر نیاز دارید یک ماژول در زمان روشن شدن سرور همیشه بالا بیاید، یک فایل با پسوند `.conf` در مسیر `/etc/modules-load.d/` می‌سازید و نام ماژول را در آن می‌نویسید.
* **بلک‌لیست کردن (Blacklisting):** اگر ماژولی مخرب است یا با سخت‌افزار تداخل دارد (مثلاً درایور گرافیک پیش‌فرض `nouveau` وقتی درایور Nvidia نصب می‌کنید)، باید آن را در پوشه `/etc/modprobe.d/` در یک فایل `.conf` با دستور `blacklist <module_name>` مسدود کنید.

---

### 🎛️ ۳. تیونینگ پارامترهای زنده کرنل (`/proc/sys` و `sysctl`)
پارامترهای عملکردی لینوکس به صورت فایل‌های مجازی در مسیر `/proc/sys/` قرار دارند.
* **تغییر موقت:** با دستور `sysctl -w key=value` یا نوشتن مستقیم مقدار با `echo` درون فایل‌های `/proc/sys/`. با ریبوت سرور از بین می‌رود.
* **تغییر دائمی:** با ساخت فایل پیکربندی در دایرکتوری `/etc/sysctl.d/` با پسوند `.conf` و اعمال آن با سوییچ `sysctl --system`. 
  * *نمونه امتحانی:* فعال‌سازی فوروارد بسته‌های شبکه: `net.ipv4.ip_forward = 1`

---

### 🚀 ۴. پیکربندی و مدیریت هسته‌ها در GRUB2
در RHEL 9 پیکربندی GRUB از ساختار **BLS (Boot Loader Specification)** پیروی می‌کند.
* **مسیر فایل ورودی‌های بوت:** فایل‌های مربوط به هر هسته در مسیر `/boot/loader/entries/` نگهداری می‌شوند.
* **فایل پیکربندی عمومی:** در مسیر `/etc/default/grub` قرار دارد.
* **ابزار `grubby`:** ابزار خط فرمانی و استاندارد رد‌هت برای دیدن لیست کرنل‌ها، مشخص کردن کرنل پیش‌فرض و اضافه یا حذف آرگومان‌های خط فرمان کرنل بدون دستکاری دستی فایل‌های متنی.

---

## ⚡ PART 2: THE ONE-CLICK ALL COMMANDS CODEBOX (کادر یکپارچه دستورات)

تمام دستورات مدیریت ماژول‌ها، بلک‌لیست، تیونینگ `sysctl` و تنظیمات گراب با `grubby` به صورت یکجا در کادر زیر قرار دارند:

```bash
# ==============================================================================
# 🔍 بخش اول: بررسی اطلاعات هسته و ماژول‌ها (INSPECTION & MODULES)
# ==============================================================================

# نمایش نسخه دقیق کرنل در حال اجرای سیستم
uname -r

# نمایش کامل معماری، نام سیستم‌عامل و نسخه کرنل
uname -a

# مشاهده لیست کامل ماژول‌های فعال و لود شده در حافظه رم
lsmod

# فیلتر کردن خروجی برای بررسی وضعیت لود بودن یک ماژول خاص (مثال: ماژول xfs)
lsmod | grep xfs

# استخراج جزئیات کامل، توضیحات، نویسنده و پارامترهای یک ماژول خاص
modinfo e1000e


# ==============================================================================
# 🛠️ بخش دوم: لود و آنلود کردن ماژول‌ها (MODPROBE)
# ==============================================================================

# لود کردن هوشمند ماژول به همراه تمام پیش‌نیازها و وابستگی‌ها در حافظه رم
sudo modprobe loop

# خارج کردن و آنلود ایمن ماژول از حافظه (تنها در صورتی که توسط پردازشی درگیر نباشد)
sudo modprobe -r loop

# لود خودکار ماژول در زمان بالا آمدن سرور (تنظیم دائم):
# ساخت فایل کانفیگ در مسیر استاندارد:
echo "overlay" | sudo tee /etc/modules-load.d/overlay.conf

# مسدود کردن (Blacklist) یک ماژول تا هرگز در زمان بوت بالا نیاید:
echo "blacklist floppy" | sudo tee /etc/modprobe.d/blacklist-floppy.conf


# ==============================================================================
# 🎛️ بخش سوم: پارامترهای زمان اجرای کرنل (SYSCTL & /PROC/SYS)
# ==============================================================================

# مشاهده مقدار یک پارامتر زنده کرنل (مثال: وضعیت فوروارد پکت‌های شبکه)
sysctl net.ipv4.ip_forward

# خواندن مستقیم مقدار پارامتر از طریق سیستم‌فایل مجازی proc
cat /proc/sys/net/ipv4/ip_forward

# نمایش لیست تمام متغیرها و تنظیمات کرنل
sudo sysctl -a

# تغییر موقت مقدار یک متغیر در رم سرور (با ریبوت سرور می‌پرد)
sudo sysctl -w net.ipv4.ip_forward=1

# دائمی کردن مقدار پارامتر کرنل (روش استاندارد آزمون RHCSA):
echo "net.ipv4.ip_forward = 1" | sudo tee /etc/sysctl.d/99-ipforward.conf

# بارگذاری و اعمال فوری تمام فایل‌های کانفیگ sysctl بدون نیاز به ریبوت
sudo sysctl --system

# اعمال دستی یک فایل پیکربندی خاص
sudo sysctl -p /etc/sysctl.d/99-ipforward.conf


# ==============================================================================
# 🚀 بخش چهارم: مدیریت هسته‌ها و بوت‌لودر با GRUBBY (GRUB2 MANAGEMENT)
# ==============================================================================

# نمایش مسیر و مشخصات کرنل پیش‌فرض سیستم (Default Kernel)
sudo grubby --default-kernel

# نمایش جزئیات کامل تنظیمات بوت مربوط به کرنل پیش‌فرض
sudo grubby --info=DEFAULT

# نمایش مشخصات و ایندکس تمام هسته‌های نصب‌شده روی سرور
sudo grubby --info=ALL

# تغییر کرنل پیش‌فرض سیستم به یک نسخه دیگر بر اساس مسیر فایل vmlinuz
sudo grubby --set-default /boot/vmlinuz-5.14.0-70.13.1.el9_0.x86_64

# افزودن یک آرگومان جدید به خط بوت تمام کرنل‌های سیستم (مثال: غیرفعال‌سازی موقت SELinux)
sudo grubby --update-kernel=ALL --args="selinux=0"

# حذف آرگومان تعیین‌شده از خط بوت تمام هسته‌ها
sudo grubby --update-kernel=ALL --remove-args="selinux=0"

# بازتولید فایل کانفیگ کلی گراب در صورت دستکاری مستقیم فایل /etc/default/grub:
# در سیستم‌های UEFI و BIOS استاندارد RHEL 9:
sudo grub2-mkconfig -o /boot/grub2/grub.cfg

```

---

## ⚡ PART 3: QUICK REFERENCE INDEX (چیت‌شیت جیبی و سریع)

| دستور / فایل | کاربرد کلیدی |
| --- | --- |
| `uname -r` | بررسی سریع نسخه کرنل در حال اجرا |
| `lsmod` | لیست کردن تمام درایورها/ماژول‌های فعال روی حافظه رم |
| `modprobe <mod>` | لود کردن امن ماژول با مدیریت خودکار وابستگی‌ها |
| `modprobe -r <mod>` | حذف و آنلود کردن ماژول از رم سرور |
| `/etc/modules-load.d/*.conf` | مسیر فایل‌های لود خودکار ماژول هنگام بوت |
| `/etc/modprobe.d/*.conf` | مسیر فایل‌های مسدودسازی (Blacklist) ماژول‌ها |
| `sysctl -a` | دیدن تمام تنظیمات اجرایی و سوئیچ‌های فعال هسته |
| `sysctl -w key=val` | اعمال تغییر موقت روی رفتار کرنل در رم |
| `sysctl --system` | بازخوانی و اعمال مجدد تمام تنظیمات فیلترهای `/etc/sysctl.d/` |
| `grubby --default-kernel` | چاپ مسیر هسته‌ای که سرور با آن روشن می‌شود |
| `grubby --update-kernel=ALL --args=".."` | تزریق یک پارامتر بوت به تمام ورودی‌های سیستم |

```

```